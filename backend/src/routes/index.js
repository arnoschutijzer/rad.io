const auth = require('./auth');
const chat = require('./chat');
const docs = require('./docs');
const rooms = require('./rooms');
const util = require('./util');

module.exports = {
  auth, chat, docs, rooms, util
};